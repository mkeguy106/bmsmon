<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE: API 31+ uses BLUETOOTH_SCAN/CONNECT; neverForLocation avoids needing location -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />
    <!-- Legacy (API <= 30) -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- GPS / fused location provider -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- Keep monitoring/logging running with the screen off via a foreground service.
         connectedDevice is the FGS type for an ongoing BLE link (Android 14+). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Android 13+: runtime-granted, for the ongoing foreground-service notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Vibration for critical low-battery alert notifications. Install-time, auto-granted. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Read connectivity (Wi-Fi vs cellular) for the locked-screen status strip. Install-time. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Network access for telemetry upload. Install-time. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Backups (SEC-7): allowBackup stays true so device-to-device migration keeps the user's
         telemetry history + settings, but the rules files exclude the GPS-tagged bms.db and the
         DataStore (deviceId/apiBaseUrl) from CLOUD backup. Cleartext (SEC-8): prod is HTTPS-only
         (the enroll UI strips schemes and defaults https; no http:// endpoint exists anywhere),
         so plaintext HTTP — which would ship signed GPS telemetry unencrypted — is disabled. -->
    <application
        android:name=".BmsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BmsMon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.BmsMon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".monitor.MonitoringService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />

        <receiver
            android:name=".BmsDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
